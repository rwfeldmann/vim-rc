set expandtab        " Use spaces instead of tabs
set shiftwidth=4     " Set the number of spaces to use for each indentation
set softtabstop=4    " Set the number of spaces a tab counts for when editing
set tabstop=4        " Set the number of spaces that a tab character represents

